For simpleSpec{1-4}.sk, run with command:

./mdsSketch simpleSpec{1-4}.sk grammar.sk <your depth> bool_expr

for simpleSpec5.sk, run with command:

./mdsSketch simpleSpec5.sk grammar.sk <your depth> expr

All files share the same input grammar file.
